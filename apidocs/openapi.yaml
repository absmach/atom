openapi: 3.0.3

info:
  title: Atom HTTP API
  version: 1.0.0
  description: |
    Frozen v1 HTTP transport contract for Atom.

    Administrative and domain operations are exposed through `POST /graphql`;
    the remaining HTTP routes are limited to authentication, health, JWKS,
    public PKI artifacts, and configured custom endpoint execution. The
    optional native and RFC 7030 enrollment routes are served from a dedicated
    TLS listener but remain part of this frozen HTTP contract. Atom also
    exposes separately frozen gRPC contracts described by
    `proto/atom/v1/atom.proto` and `proto/atom/v1/callout.proto`.

    Authenticated requests accept an ES256 JWT or Atom access token in the
    `Authorization: Bearer <token>` header. The GraphQL and session routes also
    accept the `atom_token` HttpOnly cookie. Cookie-authenticated unsafe
    requests require a trusted Origin or Referer.
  license:
    name: Apache-2.0
    url: https://www.apache.org/licenses/LICENSE-2.0

servers:
  - url: /
    description: The Atom deployment origin

tags:
  - name: Health
    description: Process liveness, dependency readiness, and metrics
  - name: Authentication
    description: Identity bootstrap, login, OAuth, sessions, and signing keys
  - name: GraphQL
    description: Domain and administrative GraphQL operations
  - name: PKI artifacts
    description: Public trust, CRL, and OCSP publication endpoints
  - name: PKI enrollment
    description: Native and RFC 7030 enrollment on the optional dedicated TLS listener
  - name: Custom endpoints
    description: Operator-configured REST projections over GraphQL

security: []

paths:
  /health:
    get:
      tags: [Health]
      operationId: legacyHealth
      summary: Readiness-compatible legacy health endpoint
      responses:
        "200":
          description: Atom is ready
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SystemStatus"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "503":
          description: At least one required component is not ready
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SystemStatus"

  /health/live:
    get:
      tags: [Health]
      operationId: live
      summary: Process liveness
      responses:
        "200":
          description: Process is alive
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LiveStatus"
        "408":
          $ref: "#/components/responses/RequestTimeout"

  /health/ready:
    get:
      tags: [Health]
      operationId: ready
      summary: Dependency readiness
      responses:
        "200":
          description: Atom is ready
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SystemStatus"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "503":
          description: At least one required component is not ready
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SystemStatus"

  /metrics:
    get:
      tags: [Health]
      operationId: metrics
      summary: Prometheus metrics when metrics are enabled
      description: This optional unauthenticated route must be network-restricted.
      responses:
        "200":
          description: Prometheus text exposition
          content:
            text/plain:
              schema:
                type: string
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "404":
          description: Metrics are disabled or unavailable in this build

  /.well-known/jwks.json:
    get:
      tags: [Authentication]
      operationId: jwks
      summary: Active ES256 verification keys
      responses:
        "200":
          description: JSON Web Key Set
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Jwks"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "429":
          $ref: "#/components/responses/RateLimited"

  /auth/public-config:
    get:
      tags: [Authentication]
      operationId: publicAuthConfig
      summary: Public authentication configuration
      responses:
        "200":
          description: Public configuration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicAuthConfig"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "429":
          $ref: "#/components/responses/RateLimited"

  /auth/signup:
    post:
      tags: [Authentication]
      operationId: signup
      summary: Register a human identity when self-registration is enabled
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SignupRequest"
      responses:
        "202":
          description: Signup accepted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SignupResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "409":
          $ref: "#/components/responses/Conflict"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/JsonUnsupportedMediaType"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "429":
          $ref: "#/components/responses/RateLimited"

  /auth/login:
    post:
      tags: [Authentication]
      operationId: login
      summary: Authenticate with a password or shared key
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Authenticated; also sets the `atom_token` HttpOnly cookie
          headers:
            Set-Cookie:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoginResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/JsonUnsupportedMediaType"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "429":
          $ref: "#/components/responses/RateLimited"

  /auth/email/verify:
    get:
      tags: [Authentication]
      operationId: verifyEmail
      summary: Consume an email-verification token
      parameters:
        - name: token
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Email verified
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [verified]
                properties:
                  verified:
                    type: boolean
                    enum: [true]
        "400":
          $ref: "#/components/responses/BadRequest"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "429":
          $ref: "#/components/responses/RateLimited"

  /auth/email/resend:
    post:
      tags: [Authentication]
      operationId: resendVerification
      summary: Request another verification email
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmailRequest"
      responses:
        "202":
          description: Request accepted whether or not the email exists
        "400":
          $ref: "#/components/responses/BadRequest"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/JsonUnsupportedMediaType"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "429":
          $ref: "#/components/responses/RateLimited"

  /auth/password/reset/request:
    post:
      tags: [Authentication]
      operationId: requestPasswordReset
      summary: Request a password-reset email
      description: |
        The body contains only the account email. Atom always builds the reset
        link from `ATOM_PASSWORD_RESET_REDIRECT`; caller-controlled redirect
        fields are rejected.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasswordResetRequest"
      responses:
        "202":
          description: Request accepted whether or not the email exists
        "400":
          $ref: "#/components/responses/BadRequest"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/JsonUnsupportedMediaType"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "429":
          $ref: "#/components/responses/RateLimited"

  /auth/password/reset:
    post:
      tags: [Authentication]
      operationId: resetPassword
      summary: Consume a reset token and replace the password
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasswordResetConfirmRequest"
      responses:
        "204":
          description: Password reset; existing sessions and credentials are revoked
        "400":
          $ref: "#/components/responses/BadRequest"
        "409":
          $ref: "#/components/responses/Conflict"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/JsonUnsupportedMediaType"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "429":
          $ref: "#/components/responses/RateLimited"

  /auth/oauth/{provider}/start:
    get:
      tags: [Authentication]
      operationId: oauthStart
      summary: Start an enabled OIDC authorization flow
      parameters:
        - $ref: "#/components/parameters/OauthProvider"
        - name: return_to
          in: query
          required: false
          schema:
            type: string
      responses:
        "307":
          description: Redirect to the provider authorization endpoint
          headers:
            Location:
              schema:
                type: string
                format: uri
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "429":
          $ref: "#/components/responses/RateLimited"

  /auth/oauth/{provider}/callback:
    get:
      tags: [Authentication]
      operationId: oauthCallback
      summary: Complete an OIDC provider callback
      parameters:
        - $ref: "#/components/parameters/OauthProvider"
        - name: code
          in: query
          schema:
            type: string
        - name: state
          in: query
          schema:
            type: string
        - name: error
          in: query
          schema:
            type: string
      responses:
        "307":
          description: Redirect to the configured success or error callback
          headers:
            Location:
              schema:
                type: string
                format: uri
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "429":
          $ref: "#/components/responses/RateLimited"

  /auth/oauth/exchange:
    post:
      tags: [Authentication]
      operationId: oauthExchange
      summary: Exchange a one-time OAuth result code for an Atom session
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OAuthExchangeRequest"
      responses:
        "200":
          description: Authenticated session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoginResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/JsonUnsupportedMediaType"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "429":
          $ref: "#/components/responses/RateLimited"

  /auth/logout:
    post:
      tags: [Authentication]
      operationId: logout
      summary: Revoke the current session and clear the auth cookie
      security:
        - bearerAuth: []
        - cookieAuth: []
      responses:
        "200":
          description: Logged out
          headers:
            Set-Cookie:
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [authenticated]
                properties:
                  authenticated:
                    type: boolean
                    enum: [false]
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "429":
          $ref: "#/components/responses/RateLimited"

  /auth/introspect:
    get:
      tags: [Authentication]
      operationId: introspect
      summary: Return the authenticated token identity
      security:
        - bearerAuth: []
        - cookieAuth: []
      responses:
        "200":
          description: Active authenticated identity
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IntrospectionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "429":
          $ref: "#/components/responses/RateLimited"

  /auth/session:
    get:
      tags: [Authentication]
      operationId: currentSession
      summary: Return the current authentication session
      security:
        - bearerAuth: []
        - cookieAuth: []
      responses:
        "200":
          description: Current session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CurrentSessionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "429":
          $ref: "#/components/responses/RateLimited"

  /auth/sessions/{id}:
    get:
      tags: [Authentication]
      operationId: getSession
      summary: Read a session visible to the caller
      security:
        - bearerAuth: []
        - cookieAuth: []
      parameters:
        - $ref: "#/components/parameters/UuidId"
      responses:
        "200":
          description: Session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "429":
          $ref: "#/components/responses/RateLimited"

  /auth/keys/rotate:
    post:
      tags: [Authentication]
      operationId: rotateSigningKeys
      summary: Rotate ES256 signing keys
      security:
        - bearerAuth: []
        - cookieAuth: []
      responses:
        "204":
          description: Keys rotated
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "429":
          $ref: "#/components/responses/RateLimited"

  /graphql:
    post:
      tags: [GraphQL]
      operationId: graphql
      summary: Execute a GraphQL operation
      description: |
        Authentication is optional at the transport boundary because a small
        set of GraphQL fields is public. Protected fields enforce authorization
        in their resolvers. The frozen GraphQL type-system contract is committed
        separately as `apidocs/graphql-schema.graphql`.
      security:
        - {}
        - bearerAuth: []
        - cookieAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GraphQLRequest"
      responses:
        "200":
          description: GraphQL response; resolver failures appear in `errors`
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphQLResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/JsonUnsupportedMediaType"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "429":
          $ref: "#/components/responses/RateLimited"

  /certs/trust-bundle.pem:
    get:
      tags: [PKI artifacts]
      operationId: trustBundle
      summary: Active and retained CA trust bundle
      parameters:
        - $ref: "#/components/parameters/IfNoneMatch"
      responses:
        "200":
          description: PEM trust bundle
          headers:
            ETag:
              schema:
                type: string
            Cache-Control:
              schema:
                type: string
          content:
            application/x-pem-file:
              schema:
                type: string
        "304":
          description: Cached representation remains current
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "429":
          $ref: "#/components/responses/RateLimited"

  /certs/issuers/{issuer_id}/crl:
    get:
      tags: [PKI artifacts]
      operationId: issuerCrl
      summary: Issuer-specific X.509 certificate revocation list
      parameters:
        - $ref: "#/components/parameters/IssuerId"
        - $ref: "#/components/parameters/IfNoneMatch"
      responses:
        "200":
          description: DER-encoded CRL
          headers:
            ETag:
              schema:
                type: string
            Cache-Control:
              schema:
                type: string
          content:
            application/pkix-crl:
              schema:
                type: string
                format: binary
        "304":
          description: Cached representation remains current
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "429":
          $ref: "#/components/responses/RateLimited"

  /certs/issuers/{issuer_id}/ocsp:
    post:
      tags: [PKI artifacts]
      operationId: issuerOcsp
      summary: Issuer-specific OCSP responder
      parameters:
        - $ref: "#/components/parameters/IssuerId"
      requestBody:
        required: true
        content:
          application/ocsp-request:
            schema:
              type: string
              format: binary
      responses:
        "200":
          description: DER-encoded OCSP response, including protocol-level error responses
          content:
            application/ocsp-response:
              schema:
                type: string
                format: binary
        "400":
          $ref: "#/components/responses/BadRequest"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "429":
          $ref: "#/components/responses/RateLimited"

  /pki/enroll:
    servers:
      - url: https://{host}:{port}
        description: Dedicated Atom enrollment TLS listener
        variables:
          host:
            default: localhost
          port:
            default: "8443"
    post:
      tags: [PKI enrollment]
      operationId: enrollCertificate
      summary: Enroll a certificate using an authenticated Atom identity
      description: |
        This route is mounted only when the dedicated enrollment TLS listener
        is enabled. A cookie-authenticated request also requires a trusted
        Origin or Referer because this is an unsafe method.
      security:
        - bearerAuth: []
        - cookieAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NativeEnrollmentRequest"
      responses:
        "200":
          description: Certificate issued, or the idempotent result replayed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnrollmentResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "409":
          $ref: "#/components/responses/Conflict"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/JsonUnsupportedMediaType"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "429":
          $ref: "#/components/responses/RateLimited"

  /pki/reenroll:
    servers:
      - url: https://{host}:{port}
        description: Dedicated Atom enrollment TLS listener
        variables:
          host:
            default: localhost
          port:
            default: "8443"
    post:
      tags: [PKI enrollment]
      operationId: reenrollCertificate
      summary: Renew a certificate by proving possession through mTLS
      description: |
        The client certificate must be presented and verified during the TLS
        handshake. OpenAPI 3.0 has no native mutual-TLS security-scheme type;
        `x-atom-client-certificate-required` is therefore part of the frozen
        transport contract.
      security: []
      x-atom-client-certificate-required: true
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NativeEnrollmentRequest"
      responses:
        "200":
          description: Certificate renewed, or the idempotent result replayed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnrollmentResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "409":
          $ref: "#/components/responses/Conflict"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/JsonUnsupportedMediaType"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "429":
          $ref: "#/components/responses/RateLimited"

  /.well-known/est/cacerts:
    servers:
      - url: https://{host}:{port}
        description: Dedicated Atom enrollment TLS listener
        variables:
          host:
            default: localhost
          port:
            default: "8443"
    get:
      tags: [PKI enrollment]
      operationId: estCaCerts
      summary: Return the RFC 7030 CA certificate bundle
      responses:
        "200":
          $ref: "#/components/responses/EstCertificates"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "429":
          $ref: "#/components/responses/RateLimited"

  /.well-known/est/simpleenroll:
    servers:
      - url: https://{host}:{port}
        description: Dedicated Atom enrollment TLS listener
        variables:
          host:
            default: localhost
          port:
            default: "8443"
    post:
      tags: [PKI enrollment]
      operationId: estSimpleEnroll
      summary: Enroll a certificate using RFC 7030 simpleenroll
      security:
        - bearerAuth: []
        - basicAuth: []
      requestBody:
        $ref: "#/components/requestBodies/EstPkcs10Request"
      responses:
        "200":
          $ref: "#/components/responses/EstCertificates"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/EstUnauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "409":
          $ref: "#/components/responses/Conflict"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/EstUnsupportedMediaType"
        "429":
          $ref: "#/components/responses/RateLimited"

  /.well-known/est/simplereenroll:
    servers:
      - url: https://{host}:{port}
        description: Dedicated Atom enrollment TLS listener
        variables:
          host:
            default: localhost
          port:
            default: "8443"
    post:
      tags: [PKI enrollment]
      operationId: estSimpleReenroll
      summary: Renew a certificate using RFC 7030 simple-reenroll and mTLS
      description: |
        The client certificate must be presented and verified during the TLS
        handshake. No HTTP Authorization credential is used.
      security: []
      x-atom-client-certificate-required: true
      requestBody:
        $ref: "#/components/requestBodies/EstPkcs10Request"
      responses:
        "200":
          $ref: "#/components/responses/EstCertificates"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/EstUnauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "409":
          $ref: "#/components/responses/Conflict"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/EstUnsupportedMediaType"
        "429":
          $ref: "#/components/responses/RateLimited"

  /.well-known/est/serverkeygen:
    servers:
      - url: https://{host}:{port}
        description: Dedicated Atom enrollment TLS listener
        variables:
          host:
            default: localhost
          port:
            default: "8443"
    post:
      tags: [PKI enrollment]
      operationId: estServerKeygen
      summary: Generate a one-time private key and enroll its certificate
      security:
        - bearerAuth: []
        - basicAuth: []
      requestBody:
        $ref: "#/components/requestBodies/EstPkcs10Request"
      responses:
        "200":
          description: One-time PKCS#8 private key and issued certificate chain
          headers:
            Cache-Control:
              schema:
                type: string
                enum: [no-store]
          content:
            multipart/mixed:
              schema:
                type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/EstUnauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "409":
          $ref: "#/components/responses/Conflict"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/EstUnsupportedMediaType"
        "429":
          $ref: "#/components/responses/RateLimited"

  /.well-known/est/csrattrs:
    servers:
      - url: https://{host}:{port}
        description: Dedicated Atom enrollment TLS listener
        variables:
          host:
            default: localhost
          port:
            default: "8443"
    get:
      tags: [PKI enrollment]
      operationId: estCsrAttrs
      summary: Return CSR attribute requirements for the authenticated identity
      security:
        - bearerAuth: []
        - basicAuth: []
      responses:
        "200":
          description: Base64-encoded DER CsrAttrs structure
          headers:
            Content-Transfer-Encoding:
              schema:
                type: string
                enum: [base64]
            Cache-Control:
              schema:
                type: string
                enum: ["private, no-store"]
          content:
            application/csrattrs:
              schema:
                type: string
                format: byte
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/EstUnauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "429":
          $ref: "#/components/responses/RateLimited"

  /api/custom/{path}:
    parameters:
      - name: path
        in: path
        required: true
        description: Configured custom endpoint path, including nested segments
        schema:
          type: string
    get:
      tags: [Custom endpoints]
      operationId: executeCustomEndpointGet
      summary: Execute a configured GET custom endpoint
      security:
        - bearerAuth: []
      responses:
        "200":
          $ref: "#/components/responses/CustomEndpointResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "429":
          $ref: "#/components/responses/RateLimited"
    post:
      tags: [Custom endpoints]
      operationId: executeCustomEndpointPost
      summary: Execute a configured POST custom endpoint
      security:
        - bearerAuth: []
      requestBody:
        $ref: "#/components/requestBodies/CustomEndpointBody"
      responses:
        "200":
          $ref: "#/components/responses/CustomEndpointResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "429":
          $ref: "#/components/responses/RateLimited"
    put:
      tags: [Custom endpoints]
      operationId: executeCustomEndpointPut
      summary: Execute a configured PUT custom endpoint
      security:
        - bearerAuth: []
      requestBody:
        $ref: "#/components/requestBodies/CustomEndpointBody"
      responses:
        "200":
          $ref: "#/components/responses/CustomEndpointResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "429":
          $ref: "#/components/responses/RateLimited"
    patch:
      tags: [Custom endpoints]
      operationId: executeCustomEndpointPatch
      summary: Execute a configured PATCH custom endpoint
      security:
        - bearerAuth: []
      requestBody:
        $ref: "#/components/requestBodies/CustomEndpointBody"
      responses:
        "200":
          $ref: "#/components/responses/CustomEndpointResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "429":
          $ref: "#/components/responses/RateLimited"
    delete:
      tags: [Custom endpoints]
      operationId: executeCustomEndpointDelete
      summary: Execute a configured DELETE custom endpoint
      security:
        - bearerAuth: []
      requestBody:
        $ref: "#/components/requestBodies/CustomEndpointBody"
      responses:
        "200":
          $ref: "#/components/responses/CustomEndpointResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "408":
          $ref: "#/components/responses/RequestTimeout"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "429":
          $ref: "#/components/responses/RateLimited"

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT or atom access token
    basicAuth:
      type: http
      scheme: basic
      description: EST username and password authentication
    cookieAuth:
      type: apiKey
      in: cookie
      name: atom_token

  parameters:
    UuidId:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    IssuerId:
      name: issuer_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    OauthProvider:
      name: provider
      in: path
      required: true
      schema:
        type: string
    IfNoneMatch:
      name: If-None-Match
      in: header
      required: false
      schema:
        type: string

  responses:
    BadRequest:
      description: Invalid request
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
        text/plain:
          schema:
            type: string
    Unauthorized:
      description: Missing or invalid authentication
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Forbidden:
      description: Authenticated caller lacks permission
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    NotFound:
      description: Object not found
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Conflict:
      description: Request conflicts with current state
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    PayloadTooLarge:
      description: Request exceeds the configured body limit
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
        text/plain:
          schema:
            type: string
    RequestTimeout:
      description: The listener's total request deadline expired
    JsonUnsupportedMediaType:
      description: Content-Type is missing or is not JSON
      content:
        text/plain:
          schema:
            type: string
    UnprocessableEntity:
      description: JSON body does not match the frozen request DTO
      content:
        text/plain:
          schema:
            type: string
    EstUnauthorized:
      description: Missing or invalid HTTP Basic, Bearer, or client-certificate authentication
      headers:
        WWW-Authenticate:
          schema:
            type: string
            example: Basic realm="Atom EST"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    EstUnsupportedMediaType:
      description: Content-Type is missing or is not application/pkcs10
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    EstCertificates:
      description: Base64-encoded DER certs-only CMS containing the issued certificate chain
      headers:
        Content-Transfer-Encoding:
          schema:
            type: string
            enum: [base64]
        Cache-Control:
          schema:
            type: string
      content:
        "application/pkcs7-mime; smime-type=certs-only":
          schema:
            type: string
            format: byte
    RateLimited:
      description: Rate limit exceeded
      headers:
        Retry-After:
          schema:
            type: integer
            minimum: 1
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    CustomEndpointResponse:
      description: Configured JSON response
      content:
        application/json:
          schema: {}

  requestBodies:
    CustomEndpointBody:
      required: false
      content:
        application/json:
          schema: {}
    EstPkcs10Request:
      required: true
      content:
        application/pkcs10:
          schema:
            type: string
            format: byte

  schemas:
    Error:
      type: object
      additionalProperties: false
      required: [error]
      properties:
        error:
          type: string

    LiveStatus:
      type: object
      additionalProperties: false
      required: [status]
      properties:
        status:
          type: string
          enum: [ok]

    ComponentStatus:
      type: string
      enum: [ok, disabled, degraded, error]

    ComponentCheck:
      type: object
      additionalProperties: false
      required: [status, message]
      properties:
        status:
          $ref: "#/components/schemas/ComponentStatus"
        message:
          type: string

    DbPoolStatus:
      type: object
      additionalProperties: false
      required: [max_connections, min_connections, acquire_timeout_secs, connect_timeout_secs, idle_timeout_secs, max_lifetime_secs, size, idle]
      properties:
        max_connections: { type: integer, minimum: 0 }
        min_connections: { type: integer, minimum: 0 }
        acquire_timeout_secs: { type: integer, minimum: 0 }
        connect_timeout_secs: { type: integer, minimum: 0 }
        idle_timeout_secs: { type: integer, minimum: 0 }
        max_lifetime_secs: { type: integer, minimum: 0 }
        size: { type: integer, minimum: 0 }
        idle: { type: integer, minimum: 0 }

    SigningKeyStatus:
      type: object
      additionalProperties: false
      required: [configured_key_id, encrypted_count, plaintext_count, total_count, plaintext_allowed]
      properties:
        configured_key_id: { type: string }
        encrypted_count: { type: integer }
        plaintext_count: { type: integer }
        total_count: { type: integer }
        plaintext_allowed: { type: boolean }

    AuditRetentionStatus:
      type: object
      additionalProperties: false
      required: [enabled, days, cleanup_interval_secs, cleanup_batch_size, last_cleanup]
      properties:
        enabled: { type: boolean }
        days: { type: integer }
        cleanup_interval_secs: { type: integer, minimum: 0 }
        cleanup_batch_size: { type: integer }
        last_cleanup:
          type: object
          nullable: true

    RateLimitPolicyStatus:
      type: object
      additionalProperties: false
      required: [category, max_requests, window_secs]
      properties:
        category:
          type: string
          enum: [auth_routes, public_routes, enrollment, graphql, custom_endpoints, admin_routes]
        max_requests: { type: integer, minimum: 0 }
        window_secs: { type: integer, minimum: 0 }

    RateLimitStatus:
      type: object
      additionalProperties: false
      required: [enabled, policies, trusted_proxy_cidrs]
      properties:
        enabled: { type: boolean }
        policies:
          type: array
          items:
            $ref: "#/components/schemas/RateLimitPolicyStatus"
        trusted_proxy_cidrs:
          type: array
          items: { type: string }

    SystemStatus:
      type: object
      additionalProperties: false
      required: [status, http_ready, grpc_ready, database, migrations, signing_keys, certificate_issuer, cache, db_pool, signing_key_state, audit_retention, rate_limits]
      properties:
        status:
          $ref: "#/components/schemas/ComponentStatus"
        http_ready:
          $ref: "#/components/schemas/ComponentCheck"
        grpc_ready:
          $ref: "#/components/schemas/ComponentCheck"
        database:
          $ref: "#/components/schemas/ComponentCheck"
        migrations:
          $ref: "#/components/schemas/ComponentCheck"
        signing_keys:
          $ref: "#/components/schemas/ComponentCheck"
        certificate_issuer:
          $ref: "#/components/schemas/ComponentCheck"
        cache:
          $ref: "#/components/schemas/ComponentCheck"
        db_pool:
          $ref: "#/components/schemas/DbPoolStatus"
        signing_key_state:
          type: object
          allOf:
            - $ref: "#/components/schemas/SigningKeyStatus"
          nullable: true
        audit_retention:
          $ref: "#/components/schemas/AuditRetentionStatus"
        rate_limits:
          $ref: "#/components/schemas/RateLimitStatus"

    Jwks:
      type: object
      additionalProperties: false
      required: [keys]
      properties:
        keys:
          type: array
          items:
            $ref: "#/components/schemas/Jwk"

    Jwk:
      type: object
      additionalProperties: false
      required: [kty, crv, x, y, kid, use, alg]
      properties:
        kty: { type: string, enum: [EC] }
        crv: { type: string, enum: [P-256] }
        x: { type: string }
        y: { type: string }
        kid: { type: string }
        use: { type: string, enum: [sig] }
        alg: { type: string, enum: [ES256] }

    PublicAuthConfig:
      type: object
      additionalProperties: false
      required: [signup_enabled, self_registration_enabled, oauth_providers, email_verification_required, dev_allow_unverified_email_login]
      properties:
        signup_enabled: { type: boolean }
        self_registration_enabled: { type: boolean }
        oauth_providers:
          type: array
          items: { type: string }
        email_verification_required: { type: boolean }
        dev_allow_unverified_email_login: { type: boolean }

    LoginRequest:
      type: object
      required: [identifier, secret]
      properties:
        identifier: { type: string }
        secret: { type: string, format: password }
        tenant_id: { type: string, format: uuid, nullable: true }
        tenant_alias: { type: string, nullable: true }
        kind:
          type: string
          enum: [password, shared_key]
          default: password

    LoginResponse:
      type: object
      additionalProperties: false
      required: [token, entity_id, session_id, expires_at, access_token, access_token_expires_at]
      properties:
        token: { type: string }
        entity_id: { type: string, format: uuid }
        session_id: { type: string, format: uuid }
        expires_at: { type: string, format: date-time }
        email_verified: { type: boolean }
        verification_required: { type: boolean }
        # issue #100: access_token/access_token_expires_at always mirror
        # token/expires_at (compatibility aliases). refresh_token and
        # refresh_token_expires_at are present only when
        # ATOM_REFRESH_TOKENS_ENABLED=true.
        access_token: { type: string }
        access_token_expires_at: { type: string, format: date-time }
        refresh_token: { type: string }
        refresh_token_expires_at: { type: string, format: date-time }

    NativeEnrollmentRequest:
      type: object
      additionalProperties: false
      required: [csr_pem, idempotency_key]
      properties:
        csr_pem:
          type: string
          description: PEM-encoded PKCS#10 certificate signing request
        ttl_secs:
          type: integer
          format: int64
          minimum: 0
          nullable: true
        idempotency_key:
          type: string

    EnrollmentResponse:
      type: object
      additionalProperties: false
      required: [credential_id, entity_id, tenant_id, issuer_id, profile_id, profile_name, identity_uri, serial_number, certificate_pem, chain_pem, not_after, renewal_threshold_seconds, renewal_due_at, idempotent_replay]
      properties:
        credential_id: { type: string, format: uuid }
        entity_id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid, nullable: true }
        issuer_id: { type: string, format: uuid }
        profile_id: { type: string, format: uuid }
        profile_name: { type: string }
        identity_uri: { type: string, format: uri }
        serial_number: { type: string }
        certificate_pem: { type: string }
        chain_pem: { type: string }
        not_after: { type: string, format: date-time }
        renewal_threshold_seconds:
          type: integer
          format: int64
          minimum: 0
        renewal_due_at: { type: string, format: date-time }
        idempotent_replay: { type: boolean }

    SignupRequest:
      type: object
      additionalProperties: false
      required: [name, email, password]
      properties:
        name: { type: string }
        email: { type: string, format: email }
        password: { type: string, format: password }
        attributes:
          type: object
          default: {}

    SignupResponse:
      type: object
      additionalProperties: false
      required: [entity_id, email, verification_required]
      properties:
        entity_id: { type: string, format: uuid }
        email: { type: string, format: email }
        verification_required: { type: boolean }

    EmailRequest:
      type: object
      required: [email]
      properties:
        email: { type: string, format: email }

    PasswordResetRequest:
      type: object
      additionalProperties: false
      required: [email]
      properties:
        email: { type: string, format: email }

    PasswordResetConfirmRequest:
      type: object
      required: [token, password]
      properties:
        token: { type: string }
        password: { type: string, format: password }
        confirm_password: { type: string, format: password, nullable: true }

    OAuthExchangeRequest:
      type: object
      required: [code]
      properties:
        code: { type: string }

    IntrospectionResponse:
      type: object
      additionalProperties: false
      required: [active, entity_id, tenant_id, session_id]
      properties:
        active: { type: boolean, enum: [true] }
        entity_id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid, nullable: true }
        session_id: { type: string, format: uuid, nullable: true }

    CurrentSessionResponse:
      type: object
      additionalProperties: false
      required: [authenticated, session]
      properties:
        authenticated: { type: boolean, enum: [true] }
        session:
          type: object
          additionalProperties: false
          required: [entityId, tenantId, sessionId, expiresAt]
          properties:
            entityId: { type: string, format: uuid }
            tenantId: { type: string, format: uuid, nullable: true }
            sessionId: { type: string, format: uuid, nullable: true }
            expiresAt: { type: string, format: date-time, nullable: true }

    Session:
      type: object
      additionalProperties: false
      required: [id, entity_id, expires_at, revoked_at, created_at]
      properties:
        id: { type: string, format: uuid }
        entity_id: { type: string, format: uuid }
        expires_at: { type: string, format: date-time }
        revoked_at: { type: string, format: date-time, nullable: true }
        created_at: { type: string, format: date-time }

    GraphQLRequest:
      type: object
      required: [query]
      properties:
        query: { type: string }
        operationName: { type: string, nullable: true }
        variables:
          type: object
          nullable: true
        extensions:
          type: object
          nullable: true

    GraphQLResponse:
      type: object
      properties:
        data:
          type: object
          nullable: true
        errors:
          type: array
          items:
            type: object
            required: [message]
            properties:
              message: { type: string }
              locations:
                type: array
                items:
                  type: object
              path:
                type: array
                items: {}
              extensions:
                type: object
