# Documentation archive

This directory preserves superseded product drafts and implementation plans for
historical context. It is not published as product documentation and is not a
source of truth.

Current user, operator, architecture, and development documentation lives in
[`docs/content/docs`](../content/docs). Frozen machine-readable API contracts
live in [`apidocs`](../../apidocs).
